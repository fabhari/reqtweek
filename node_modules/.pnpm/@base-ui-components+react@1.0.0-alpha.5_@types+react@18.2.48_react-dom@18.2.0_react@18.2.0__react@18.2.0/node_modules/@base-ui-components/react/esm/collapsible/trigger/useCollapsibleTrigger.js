'use client';

import * as React from 'react';
import { mergeReactProps } from '../../utils/mergeReactProps.js';
import { useForkRef } from '../../utils/useForkRef.js';
import { useButton } from '../../use-button/index.js';
export function useCollapsibleTrigger(parameters) {
  const {
    panelId,
    disabled,
    open,
    rootRef: externalRef,
    setOpen
  } = parameters;
  const {
    getButtonProps,
    buttonRef
  } = useButton({
    disabled,
    focusableWhenDisabled: true,
    type: 'button'
  });
  const handleRef = useForkRef(externalRef, buttonRef);
  const getRootProps = React.useCallback((externalProps = {}) => getButtonProps(mergeReactProps(externalProps, {
    type: 'button',
    'aria-controls': panelId,
    'aria-expanded': open,
    disabled,
    onClick() {
      setOpen(!open);
    },
    ref: handleRef
  })), [panelId, disabled, getButtonProps, handleRef, open, setOpen]);
  return {
    getRootProps
  };
}